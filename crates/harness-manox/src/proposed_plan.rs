//! `<proposed_plan>` block streaming parser + extractors.
//!
//! The implementation moved to `agent::proposed_plan` (shared with the pi
//! harness's plan-review flow); this module keeps the archived import path
//! alive.

pub use agent::proposed_plan::*;
