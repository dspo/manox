//! Skills — model-invokable reference documents.
//!
//! The implementation moved to `agent::skill` (shared with the pi path's
//! slash-command and mention surfaces); this module keeps the archived
//! import path alive.

pub use agent::skill::*;
