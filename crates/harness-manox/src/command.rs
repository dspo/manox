//! Slash commands — user-triggered prompt macros.
//!
//! The implementation moved to `agent::command` (shared with the pi path's
//! slash-command surface); this module keeps the archived import path alive.

pub use agent::command::*;
