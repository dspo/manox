//! Frontmatter parsing for markdown definition files.
//!
//! The implementation moved to `agent::frontmatter` (shared with the pi
//! path); this module keeps the archived import path alive.

pub use agent::frontmatter::*;
