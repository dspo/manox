//! `TerminalSettings` — shell / font / cursor / scrollback / env / etc.
//!
//! Stage 8 wires the full settings group into `agent::settings::Settings` as a
//! `terminal` subfield. Stage 0 leaves the module empty so the crate compiles.
