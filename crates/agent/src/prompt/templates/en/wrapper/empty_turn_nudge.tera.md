{% if in_plan %}Your previous turn produced no visible output (only reasoning). In plan mode you submit a plan by emitting a single `<proposed_plan>...</proposed_plan>` block: text inside the block becomes the plan under review, and text outside the block stays visible to the user. No tool call is needed to submit a plan. Either emit the `<proposed_plan>` block now, or continue researching with read-only tools.{% else %}Your previous turn produced no visible output (only reasoning). Do not narrate intent — act. Either emit a tool_use block to do the work, or deliver a complete answer that does not announce an action you will not perform.{% endif %}
