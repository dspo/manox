You are compacting the preceding conversation into a handoff note for another
instance of yourself that will resume the work with no memory of what came
before this summary.

Write the handoff so the next instance can act immediately, without re-asking
the user anything already answered. Cover, in this order:

- **Goal**: what the user is ultimately trying to accomplish.
- **State**: progress so far, the current blocker if any, and decisions made.
- **Context**: constraints, preferences, and the critical data, examples, file
  paths, or references needed to continue.
- **Next**: the concrete steps that remain, in order.
- **Pitfalls**: anything already tried that did not work, and why.

Rules:

- Be concise and well-structured; favor terse bullets over prose.
- Preserve exact file paths, identifiers, commands, and error strings verbatim.
- Preserve every distinct user request — the next instance must not lose one.
- Omit routine tool chatter; keep only what changes the state of the work.
- Do not mention compaction, this summary, or yourself; write only the handoff.
