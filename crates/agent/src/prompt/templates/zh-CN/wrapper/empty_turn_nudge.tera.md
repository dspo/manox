{% if in_plan %}你上一轮没有产出可见输出（仅有推理）。在 Plan 模式下，你通过发出一个 `<proposed_plan>...</proposed_plan>` 块来提交计划：块内文本成为待审阅的计划，块外文本对用户保持可见。提交计划不需要调用任何工具。现在要么发出 `<proposed_plan>` 块，要么继续用只读工具调研。{% else %}你上一轮没有产出可见输出（仅有推理）。不要叙述意图——行动。要么发出 tool_use 块来完成工作，要么给出完整的答案，且不宣布你不会执行的动作。{% endif %}
