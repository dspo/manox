The current conversation title is: "{{ current_title }}". If the latest user message indicates a NEW conversation topic distinct from the current title, output a concise new title (a few words, no trailing punctuation, no surrounding quotes, no prefix such as "Title:", in the same language as the user's latest message, on a single line). Otherwise output exactly the single word {{ unchanged_sentinel }}.
